import styles from "../style";
import { footerLinks, socialMedia } from "../constants";

const Footer = () => {
  return <section className={`${styles.flexCenter}`}></section>;
};

export default Footer;
