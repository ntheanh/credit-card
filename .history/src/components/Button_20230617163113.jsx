import React from "react";

const Button = ({ styles }) => {
  return (
    <button className={`py-4 px-6 font-poppins font-medium bg-blue-gradient `}>
      Get Started
    </button>
  );
};

export default Button;
